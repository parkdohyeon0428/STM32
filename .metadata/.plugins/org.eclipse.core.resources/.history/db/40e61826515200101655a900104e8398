/*
 * motor.c
 *
 *  Created on: Jun 26, 2025
 *      Author: kccistc
 */

#include "motor.h"

void Motor_Init(TIM_HandleTypeDef *htim, uint32_t channel)
{
	hmotorTim = htim;
	motorTimChannel = channel;
}

void Motor_SetFreq(uint32_t freq)
{
	if (freq < 100) freq = 100;
	else if (freq > 10000) freq = 10000;

	__HAL_TIM_SET_AUTORELOAD(&htim1, 1000000/freq - 1);
	__HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_2, 1000000/freq/2 - 1);
}

void Motor_Start()
{
	HAL_TIM_PWN_Start(hmotorTim, motorTimChannel);
}

void Motor_Stop()
{
	HAL_TIM_PWN_Stop(hmotorTim, motorTimChannel);
}
